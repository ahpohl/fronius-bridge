# Since Mosquitto 2.x the stock container binds localhost-only and rejects
# anonymous clients; this reopens it for the compose network. The broker is
# not published to the host, so only containers on the network can connect.
# Deployments that need authentication or TLS run an external broker and set
# it in their config.yaml instead.
listener 1883
allow_anonymous true
